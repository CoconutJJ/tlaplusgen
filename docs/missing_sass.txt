Unsupported SASS instructions found in sliced CFGs across all example kernels.
Generated by running sass2tla.py with both builtin and gpucode backends on
examples/*.sass and examples/fp_ops/*.sass with pattern WARPSYNC|USETMAXREG.

Float conversion:
  HFMA2              packed half-precision fused multiply-add on two FP16 values

Integer predicate:
  UISETP.NE.OR       dst = (src1 != src2) OR pred (uniform)

Memory loads:
  LDS                load 32-bit from shared memory
  LDS.128            load 128-bit from shared memory
  LDS.U8             load unsigned 8-bit from shared memory

Uniform address:
  ULEA               uniform load effective address: dst = (src << shift) + b

Uniform predicate logic:
  UPLOP3.LUT         uniform predicate 3-input LUT
